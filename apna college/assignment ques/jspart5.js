/**Create a program that generates a random number representing  dice roll. */

let diceOutcome = Math.floor(Math.random() * 6) + 1;
console.log(`the number on dice is ${diceOutcome}`);

/**Q2. Create an object representing a car that stores the following properties for the car, car: name,model,color. */

let car = {
  bmw: ["X5", "M-sport", "blue"],
  audi: ["A8L", "Quatro", "black"],
};

/**Q3. Create an object Person with their name, age and city.*/
