// we know some basic Email validation rules according to that we are going to make a program for that.

const email=prompt("Enter the email");
console.log(email);

const emailLen=email.length;
console.log(emailLen);